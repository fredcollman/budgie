<%= form_for :tag, url: tags_path do |f| %>
  <div class="input-field">
    <%= f.label :name %>
    <%= f.text_field :name, length: 30, required: "required", value: @name %>
  </div>
  <div class="input-field">
    <%= f.label :description %>
    <%= f.text_area :description, class: "materialize-textarea", value: @description %>
  </p>
  <%= render 'application/submit_button', text: 'Save' %>
<% end %>
